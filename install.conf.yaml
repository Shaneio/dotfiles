- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/tmux/tmux.conf:
      create: true
      path: tmux/tmux.conf
    ~/.config/tmux/tmuxp/:
      create: true
      path: tmux/tmuxp/

#- create:

#- shell:
